{
  "enabledPlugins": {
    "context-mode@context-mode": true
  },
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'REPO_ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0; for sentinel in /tmp/dechord-notify-*.pending; do   [ -f \"$sentinel\" ] || continue;   \"$REPO_ROOT/ops/scripts/send-telegram-summary.sh\" --sentinel-file \"$sentinel\" && rm -f \"$sentinel\"; done; exit 0'"
          }
        ]
      }
    ]
  }
}
