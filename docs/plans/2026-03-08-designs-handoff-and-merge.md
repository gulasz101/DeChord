# Designs Handoff And Merge

## Task Checklist

- [x] Prepare a reusable external-agent prompt covering the prototype redesign requirements and constraints.
- [x] Merge `codex/designs-prototypes` back into `main`.
- [x] Verify the merged result still builds for the five design directories.
- [x] Run `make reset` before final handoff.

## Notes

- The reusable prompt is delivered in the assistant handoff rather than stored as a repo artifact.
- The merge target is `main`, per user instruction.
